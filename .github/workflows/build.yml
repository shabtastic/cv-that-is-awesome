name: Build Awesome-CV
on:
  push:
    branches: [ main ]

jobs:
  build_cv:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Compile LaTeX
        uses: xu-cheng/latex-action@v3
        with:
          root_file: cv.tex
          compiler: xelatex
          args: "-pdf -interaction=nonstopmode -halt-on-error"

      - name: Upload PDF
        uses: actions/upload-artifact@v4
        with:
          name: ShabnamHakimi-CV
          path: cv.pdf