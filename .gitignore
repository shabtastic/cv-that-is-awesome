# LaTeX build artifacts
*.aux
*.bbl
*.bcf
*.blg
*.log
*.out
*.run.xml
*.toc
cvbibcounts.tex
cv-preset.tex

# Compiled PDFs (built by CI; not tracked in source)
cv-full.pdf
cv-full-public.pdf
cv-industry.pdf
cv-speaking.pdf
cv.pdf

# cv-preset.tex IS tracked (it's the committed fallback default).
# The Makefile overwrites it at build time but the repo always has
# a valid copy (config/full.tex) so fresh clones compile cleanly.

# Python
__pycache__/
*.pyc
.env
.pytest_cache/

# macOS
.DS_Store
config/.DS_Store
fonts/.DS_Store
refs/.DS_Store
sections/.DS_Store
scripts/.DS_Store


